# Ignored native build scripts (better-sqlite3, esbuild, sharp, ...) must not
# turn an install/run into a hard failure on this Windows dev host, where the
# backend's native deps are intentionally not built. The implicit pre-run
# install is itself disabled via `verifyDepsBeforeRun: false` in
# pnpm-workspace.yaml.
strict-dep-builds=false
